# bv (beads viewer) local config and caches
.bv/

# Node / build outputs
node_modules/
out/
dist/
release/
*.app/
.vite/
coverage/

# Logs and editor noise
*.log
.DS_Store
.vscode/
.idea/

# Environment
.env
.env.local
.env.*.local

# Per-machine agent instructions (never shared/committed)
CLAUDE.local.md

# remote-helper local `go build` output (the dist/ build artifacts are ignored
# in remote-helper/.gitignore — generated by build.sh, not checked in).
/remote-helper/remote-helper

