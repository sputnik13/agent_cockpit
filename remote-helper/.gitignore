# Build output (static binaries + manifest), produced by build.sh, which runs
# automatically via the predev/prebuild npm hooks. These are generated build
# artifacts and are NOT checked in; electron-builder bundles the freshly-built
# dist/ at package time. Go is therefore a required build dependency.
/dist/
